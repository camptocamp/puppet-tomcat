# file installed but NOT managed by Puppet
<%= @basedir %>/logs/catalina.out {
      copytruncate
      weekly
      size 500M
      dateext
      rotate 53
      compress
      delaycompress
      missingok
      create 644 tomcat tomcat-admin
}
